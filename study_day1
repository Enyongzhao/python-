2023.8.22
Day2内容
flag的使用，用于比较 得出结果布尔值，如flag1 = 2 == 1，其中flag1结果为False
print的时候使用%d 将要输出的变量按顺序用括号写出，中间需要一个%
and和or直接写，不需要使用&&或者||

Day3内容
分支结构，选择结构
if elif else
if语句和else语句后面需要加:
print('f(%.2f) = %.2f' % (x, y)) 其中%占位置，2f表示2位小数（浮点数需要使用%f）
练习:判断能否组成三角形，需要先使得参数类型变为float，否则结果会出现错误
